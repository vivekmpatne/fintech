name: hackenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - jupyterlab
  - ipykernel
  - pip:
      - fastapi
      - uvicorn[standard]
      - shap
      - joblib
      - pydantic
